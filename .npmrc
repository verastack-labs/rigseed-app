# Fail rather than silently resolving a dependency nobody declared.
strict-peer-dependencies=false
auto-install-peers=true
resolution-mode=highest

# Keep the layout strict. A phantom dependency should be an error at dev time,
# not a mystery at build time.
node-linker=isolated
shamefully-hoist=false
