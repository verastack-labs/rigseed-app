A single figure with its label and context line.

```jsx
<StatCard icon={<Icon name="download" />} label="Down speed"
          value="4.2 MiB/s" sub="from 24 seeds" tone="accent" />
```

Lay them out in a 4-column grid with a 12px gap.
