Progress track used in every torrent row, card and detail header.

```jsx
<ProgressBar value={64} showValue />
<ProgressBar value={100} tone="accent2" height={4} />
<ProgressBar value={38} paused showValue />
```

Always animate the width between polls - never let it jump.
