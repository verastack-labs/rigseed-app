Mono span at the right size and tone.

```jsx
<DataValue size="xl" tone="accent">4.2 MiB/s</DataValue>
<DataValue size="xs" tone="dimmer">torrents/add · app/preferences</DataValue>
```

Download figures take `accent`, upload figures take `accent2`, endpoint names take `dimmer`.
