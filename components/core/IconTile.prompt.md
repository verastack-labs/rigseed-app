Tinted icon backdrop - the recurring "this is a thing" affordance.

```jsx
<IconTile size={30} color="var(--swatch-sage)"><Icon name="film" /></IconTile>
<IconTile size={46}><Icon name="inbox" /></IconTile>
```

Background is always the colour at 18% - never a solid fill.
