Flat surface container - the default way to group anything.

```jsx
<Card title="Alternative rate schedule" api="scheduler_enabled" action={<Switch checked />}>
  …
</Card>
<Card padded={false}>{/* a table manages its own padding */}</Card>
```

Never add a shadow to a resting card. Shadows are for menus, the FAB, modals and the expanded rail.
