Row and file selection box. Accent fill with an accent-on tick at 3.4 stroke width.

```jsx
<Checkbox checked={sel.includes(hash)} onChange={() => toggle(hash)} />
<Checkbox indeterminate size={20} />
```
