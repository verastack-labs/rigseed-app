Exclusive choice strip on a surface2 track — selected item takes accent-soft.

```jsx
<SegmentedControl
  options={[{value:'easy',label:'Easy'},{value:'grid',label:'Grid'},{value:'list',label:'List'}]}
  value={layout} onChange={setLayout} />
```

Two to five options. Longer lists become a list or a dropdown, not a wider strip.
