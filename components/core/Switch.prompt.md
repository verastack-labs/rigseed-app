The 36×20 preference toggle. Knob travels 16px on `cubic-bezier(0.34,1.56,0.64,1)`.

```jsx
<Switch checked={atm} onChange={setAtm} label="Automatic torrent management" />
```

Switches are for settings. Checkboxes are for selecting rows and files.
