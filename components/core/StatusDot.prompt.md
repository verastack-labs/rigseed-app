Dot + word status indicator.

```jsx
<StatusDot tone="accent2" label="working" />
<StatusDot tone="accent" label="querying 3 engines" pulse />
```

Tracker and log conventions: working `accent2`, updating `accent`, not contacted `muted`, error `danger`, warning `warn`.
